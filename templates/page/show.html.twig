{% extends 'base.html.twig' %}

{% block title %}Page{% endblock %}

{% block body %}
    <h1>Page</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ page.id }}</td>
            </tr>
            <tr>
                <th>Titre_page</th>
                <td>{{ page.titrePage }}</td>
            </tr>
            <tr>
                <th>Color_titre_page</th>
                <td>{{ page.colorTitrePage }}</td>
            </tr>
            <tr>
                <th>Bg_color</th>
                <td>{{ page.bgColor }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('page_index') }}">back to list</a>

    <a href="{{ path('page_edit', {'id': page.id}) }}">edit</a>

    {{ include('page/_delete_form.html.twig') }}
{% endblock %}
